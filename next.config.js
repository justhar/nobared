/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com', 'private-user-images.githubusercontent.com', 'i.pravatar.cc'],
    },
  }
  module.exports = nextConfig